#include <iostream>
#include <cmath>

int main(){
    double x = 3;
    double y = 4;
    double z;

    //z = std::max(x, y);
    //z = std::min(x, y);

    // Potencia, 2 elevado a 4
    //z = pow(2,4);

    //Square root
    //z = sqrt(9);

    // Valor absoluto, distancia de 0
    //z = abs(-3);

    double i = 3.14;
    //z = round(i);
    //z = ceil(i);
    z = floor(i);

    std::cout << z << "\n";

    // cplusplus.com/references/cmath

    return 0;
}